"""Utilidades compartidas por las notas del laboratorio.

- `report`  — tabla comparativa en markdown + verificación con `assert`.
- `ref`     — referencias numéricas independientes (numpy puro; **no** importa
              `rukan` ni `openseespy`).
- `svg`     — emisor de figuras SVG en el estilo del blog.
- `publish` — copia las figuras generadas a `struct_pad/public/<slug>/`.
"""
